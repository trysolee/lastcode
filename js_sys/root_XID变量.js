// XID 里面的数据 , 本地不能修改  
// 由服务器返回数据 设置
// 本地只能读取 ,

SYS.XID.注册([
	'家庭ID', '数', 0
]);

SYS.XID.注册([
	'家庭名称', '串', ''
]);

SYS.XID.注册([
	'我的ID', '数', 0
]);

SYS.XID.注册([
	'我的昵称', '串', ''
]);

SYS.XID.注册([
	'我的用户名', '串', ''
]);

SYS.XID.注册([
	'我是管理员', '布尔', false
]);

SYS.XID.注册([
	'上传进度', '数', 10000
	// 10000 : 没有上传进度
	// -1 ~ -9999 : 上传文件的编号
]);