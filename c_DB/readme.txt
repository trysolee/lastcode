
------------------------------ DB地图

js_DB地图
php_DB地图


为方便生成（服务端php）和（终端js）文件
先创建一个中间文件，叫做（xx地图）

至于是否需要分成js和php两个（地图文件）
就有待考虑

------------------------------js_BX

终端的js对象，包含（终端BX对象）的各种方法


------------------------------php_BX

服务端的php对象，包含（服务端BX对象）的各种方法


------------------------------返回view_html

生成返回页面的html
针对（DB）里面的（view）（edit）（new）

------------------------------权限

执行语句块
再判断（后台控制.返回正常）

------------------------------变量名转换

在同一个<DB>里面 , 用一个字母代替,
但是当遇到"孩子id" 这一类变量, 就要小心处理
必须保证"全局同名" , 不然在<页堆>找不到





